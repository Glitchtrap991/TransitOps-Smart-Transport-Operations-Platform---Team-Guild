{
    "installCommand": "cd TransitOps/frontend && npm install",
    "buildCommand": "cd TransitOps/frontend && npm run build",
    "outputDirectory": "TransitOps/frontend/dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}